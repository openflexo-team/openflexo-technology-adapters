dependencies {
  // openflexo dep
  compile project(":xmlconnector")

  compile flexoUi()
  compile flexoOntologyUi()

  runtime fmlTechnologyAdapterUi()
  runtime group: "org.openflexo", name: "fml-technologyadapter-ui", version: "1.9.0-SNAPSHOT"
  // external dep
  // testing dep
    testCompile flexoUiTest()
}
tasks.withType(Test) {
	jvmArgs += ["-Djava.awt.headless=true"]
}