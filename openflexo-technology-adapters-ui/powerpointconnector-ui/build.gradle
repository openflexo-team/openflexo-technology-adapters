dependencies {
    compile project(":powerpointconnector")

  // openflexo dep
   compile flexoUi()
   runtime fmlTechnologyAdapterUi()
   
  // external dep
  // testing dep
    testCompile flexoUiTest()
}
tasks.withType(Test) {
	jvmArgs += ["-Djava.awt.headless=true"]
}